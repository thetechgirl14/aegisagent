# AegisAgent — Python dependencies
# Install with: pip install -r requirements.txt

# Azure OpenAI SDK (includes async support)
openai>=1.30.0

# Azure AI Content Safety — Prompt Shield + harmful content analysis
azure-ai-contentsafety>=1.0.0

# Pydantic — used for L2SecurityVerdict schema (v2 preferred, v1 fallback handled in code)
pydantic>=2.0.0

# Environment variable loading
python-dotenv>=1.0.0

# Dashboard UI
streamlit>=1.35.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
